/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  com.seeyon.boot.starter.nacos.annotation.ConfigAutoRefresh
 *  org.slf4j.Logger
 *  org.slf4j.LoggerFactory
 *  org.springframework.boot.context.properties.ConfigurationProperties
 */
package com.seeyon.boot.starter.spi;

import com.seeyon.boot.starter.nacos.annotation.ConfigAutoRefresh;
import java.util.ArrayList;
import java.util.List;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties(prefix="seeyon.spi")
@ConfigAutoRefresh
public class SPIProperties {
    private static final Logger log = LoggerFactory.getLogger(SPIProperties.class);
    public static final String PREFIX = "seeyon.spi";
    private boolean enable = true;
    private List<String> spiPlugins = new ArrayList<String>();

    public boolean isEnable() {
        return this.enable;
    }

    public List<String> getSpiPlugins() {
        return this.spiPlugins;
    }

    public void setEnable(boolean enable) {
        this.enable = enable;
    }

    public void setSpiPlugins(List<String> spiPlugins) {
        this.spiPlugins = spiPlugins;
    }
}
